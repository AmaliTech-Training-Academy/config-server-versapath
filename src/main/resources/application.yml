server:
  port: ${SERVER_PORT}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_GIT_URL}
          clone-on-start: ${CLONE_ON_START}
          default-label: ${CONFIG_SERVER_GIT_DEFAULT_LABEL}
